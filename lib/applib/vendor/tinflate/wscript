def configure(conf):
    pass

def build(bld):
    bld.stlib(source=['tinflate.c'],
              export_includes='.',
              target='tinflate',
              use='pblibc fw_includes')

# vim:filetype=python
